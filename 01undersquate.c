# include  <stdio.h>

int main(){
        int length, breath;
        printf("WHat is teh length of the rectangle \n");
        scanf("%d", &length);
        printf("WHat is teh length of the rectangle \n");
        scanf("%d",&breath);
         printf("WHat is teh length of the rectangle %d \n",length*breath);
        return 0;
    }